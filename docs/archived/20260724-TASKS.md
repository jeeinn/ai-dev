# 任务清单（写路径交付 + Agent 摩擦 + Bootstrap，已归档）

> **状态：已归档（2026-07-24）**  
> 现行任务清单见 [`../TASKS.md`](../TASKS.md)（仅保留未完成待办）。  
> 本文为 Issue #4 实跑复盘后交付的 **写路径 fail-closed / Agent 摩擦 / Bootstrap 文件日志 / 轻量可观测** 完整记录。  
> 更早阶段：P0–P2 核心 → [`20260716-TASKS.md`](20260716-TASKS.md)；P3 开源 + 加固 → [`20260723-TASKS.md`](20260723-TASKS.md)。

---

> 更新：2026-07-24  
> 产品边界：**Gitea 优先** · 内置 Agent 默认可用 · OpenCode 可选 · 不做多托管平台抽象  
> 交付分支：`fix/write-path-push-and-agent-friction`（已合入 master，PR #19）  
> 决策：[20260714-coding-gateway-multi-vcs.md](20260714-coding-gateway-multi-vcs.md)

---

## 演进主线（本阶段）

```text
P0–P2（已归档）→ P3 开源 + 加固（已归档）
        │
        └─► 写路径交付正确性 + Agent 摩擦 + Bootstrap 文件日志 + 轻量可观测（本阶段，已交付）
```

---

## 实跑复盘：Issue #4（2026-07-24）

来源：[jeeinn/ai-dev#4](http://182.92.129.124:3000/jeeinn/ai-dev/issues/4) · Task #6 `solve_comment` · session `619286bd` · 分支 `ai/dev/issue-4`

### 结论（核实）

| 项 | 事实 |
|----|------|
| 本地 commit | **有** — agent 经 `run_command` 完成 `git commit`（先因缺 `user.name/email` 失败一次，配置后成功） |
| Push | **无** — 日志无 `git push`；启动时即 `Branch ai/dev/issue-4 is local-only` |
| PR | **未开成** — `PR finalize on clean tree failed: create PR: API error 404` |
| Issue 观感 | agent 评论「完成」+ `Task 6 completed successfully` / `action=comment`，**静默降级** |

根因：`BuildSolveToolPrompt` 声明网关负责 commit/push/开 PR，但 agent 自行 commit 后工作区干净，`finalizeWriteChanges` 走 clean-tree 路径时**直接 CreatePR、不 Push**；远端无 head 分支 → Gitea 404 → 仍按成功评论写回。

### 过程中的其它摩擦

| 现象 | 说明 | 本阶段 |
|------|------|--------|
| `unknown tool: ls` | 模型把 `ls` 当 tool 名 | 已修 |
| `cd /workspace` / 错误绝对路径 | 猜 cwd | 已修 |
| `git commit` exit 128 | 缺 git identity | 已修 |
| `git apply` / `apply_diff` 失败 | 改文件路径不稳 | 未单独立项 |
| 分析报告未完全落地 | 建议改 README，实现可能只改 archived | soft gate 文案已做 |

---

## P0 — 写路径交付正确性

> 目标：本地有 commit 时，必须 push 成功并开出 PR（或明确失败），禁止「评论成功、远端无物」。

- [x] **clean-tree 先 Push 再 CreatePR**  
  `finalizeWriteChanges` 在 `!git.HasChanges()` 且分支 ≠ base 时先 `git.Push`，再 `finalizeWriteTaskPR`；push 失败不得假装成功。
- [x] **PR / push 失败不标 success**  
  finalize 失败返回 error（fail-closed）；`admin` Gitea client 不可用时同样失败。
- [x] **约束 agent 自行 commit/push**  
  Prompt 强化：不要自己 `git commit` / `git push`，交给网关 finalize。  
  （可选未做：sandbox 拦截 agent 侧 commit — 仍在现行 backlog。）

---

## P1 — Agent 摩擦

- [x] **`unknown tool` 错误附带可用工具名**
- [x] **常见 shell 别名自动改写**  
  `ls`/`dir` → `list_files`；`pwd` → `run_command`；`cat`/`type`（带 path）→ `read_file`。无法安全改写时错误信息附 hint。
- [x] **预置 git identity**  
  `user.name` = Gitea 用户名；`user.email` = `{gitea_username}@matea.local`。
- [x] **注入真实 workspace 路径**  
  写任务 / analyze loop 注入 `WorkDir`；analyze single-shot 标明无本地工作区。

---

## P1 — Bootstrap / 运维体验

### Bug：最小 bootstrap 未开启文件日志（已修复）

修复前：bootstrap 仅有 `logging.level: info`，无 `logging.path` → 仅 stdout。  
修复后：写入 `logging.path: "./data"` → `./data/matea.log`；README / DEPLOYMENT 已同步。

- [x] **bootstrap 增加默认 `logging.path`**
- [ ] （可选）bootstrap 启动日志打印「Logging to file: …」— 仍在现行 backlog

---

## P2 — 可观测性 / 完整性（轻量项）

- [x] **Issue 评论区分**「PR 已创建」vs「更新已有 PR」（✅ / 🔄）
- [x] **分析建议落地提示（soft gate 文案）**  
  （未做：文件级 checklist / README 链接自动核对 — 仍在现行 backlog。）
- [x] **session 分支 local-only 日志警告**  
  （可选 WebUI 高亮 — 仍在现行 backlog。）
