# Linux scripts

Placeholder for bash/shell helpers used on Linux CI or local Unix hosts.

| Script | Purpose |
|--------|---------|
| `run-tests.sh` | Run unit + selected integration tests with CGO |
| `test-webhook.sh` | Smoke-post a sample Gitea webhook payload |

Cross-platform Go helpers live in [`../common/`](../common/).
Windows E2E PowerShell scripts live in [`../windows/`](../windows/).
